@using PLDMS.BL.DTOs
@model AddStudentsRequest

<div id="addStudentModal" class="fixed inset-0 z-50 hidden overflow-y-auto">
    <div class="student-modal-overlay fixed inset-0 bg-gray-900/50 transition-opacity"></div>
    
    <div class="flex items-center justify-center min-h-screen p-4">
        <div class="relative bg-white rounded-xl shadow-xl w-full max-w-2xl overflow-hidden flex flex-col max-h-[80vh]">
            
            @* <div id="globalErrorContainer" class="hidden mb-4 p-3 bg-red-50 border border-red-200 rounded-lg flex items-center gap-2 text-red-700 text-sm"> *@
            @*     <i class="ph ph-warning-circle text-lg"></i> *@
            @*     <span data-valmsg-for="Error"></span> *@
            @* </div> *@
            
            <div class="p-6 border-b border-gray-100 flex justify-between items-center bg-white sticky top-0">
                <h3 class="text-xl font-bold text-gray-800">Add Students to Cohort</h3>
                <button type="button" class="student-x-btn text-gray-400 hover:text-gray-600">
                    <i class="ph ph-x text-2xl"></i>
                </button>
            </div>

            @* <div class="p-4 bg-gray-50 border-b border-gray-100"> *@
            @*     <div class="relative"> *@
            @*         <i class="ph ph-magnifying-glass absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"></i> *@
            @*         <input type="text" id="studentSearchInput" *@
            @*                placeholder="Search by name or email..." *@
            @*                class="w-full pl-10 pr-4 py-2 border border-gray-200 rounded-lg focus:ring-2 focus:ring-brand-red/20 outline-none transition-all"> *@
            @*     </div> *@
            @* </div> *@

            <div class="flex-1 overflow-y-auto p-4" id="studentListContainer">
                <div class="flex justify-center py-8">
                    <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-brand-red"></div>
                </div>
            </div>

            <div class="bg-gray-50 px-6 py-4 border-t border-gray-200 flex justify-between items-center">
                <span class="text-sm text-gray-500" id="selectedCountText">0 students selected</span>
                <div class="flex gap-3">
                    <button type="button" class="student-cancel-btn px-4 py-2 text-gray-600 hover:bg-gray-200 rounded-lg">Cancel</button>
                    <button type="button" id="confirmAddStudents" class="bg-brand-red text-white px-6 py-2 rounded-lg font-medium shadow-sm hover:bg-red-700 transition-all">
                        Save Changes
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>